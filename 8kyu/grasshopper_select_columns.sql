/*
 * Grasshopper Select Columns - SQL
 *
 * Using only SQL, write a query that returns all rows in the custid, custname, and custstate
 * columns from the customers table.
 */
 SELECT custid, custname, custstate FROM customers;